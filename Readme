This repository contains the VBA assignment which entails of:

 - Screenshots of each worksheet from the code being run.
 - The macro enabled spreadsheet with the code run through.
 - VBA BAS file (the VBA code exported).
 - Word document that also contains the VBA code.
